package chap07.protect.package1;

import chap07.protect.A;

public class InnerClass1 {
	
	public static void main( String[] args ) {
		
		// A a = new A();
		// A 클래스는 public 이지만 생성자 A 는 protected 이기 때문에
		// 인스턴스를 생성할 수 없다.
		// a.method();
		// method() 메서드도 protected 이기 때문에 호출이 불가능하다.
		
	}
}
